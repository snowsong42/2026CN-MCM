# C题建模方案复核与算法定稿

本稿在重新阅读题目和初稿后，补做了附件结构检查、简单历史预测对照与价格关联诊断。它是本轮推荐方案的主版本；原稿的数据说明和输出要求仍可参考。

“定稿”指明确建模路线，不表示已经证明某预测模型在所有算法中误差最小。第一问可以在明确假设下证明优化模型的适配性；第二至四问的最终策略优劣仍须按时间顺序回测。当前没有求解全年购电策略，没有完成结果Excel，也没有计算节费率。

## 1. 本轮确定的模型组合

| 问题 | 首选完整方案 | 第二方案及定位 |
|---|---|---|
| 1 | **确定性线性规划（LP）**，采用HiGHS的对偶单纯形或内点法 | **混合整数线性规划（MILP）**，用于显式充放电互斥和结果交叉核对 |
| 2 | **周期结构预测＋因果储能策略约束的场景随机MILP（SAA）** | **校准分位数预测＋确定性MILP**，作为简洁、可执行的对照 |
| 3 | **带不对称结算的多阶段随机模型预测控制（SMPC）**，各次优化用场景MILP | **带误差缓冲的确定性MPC**，作为计算量较小的基线 |
| 4 | **净负荷驱动的价格回归＋联合场景随机优化／SMPC**，分别保持问题2、3的交易权限 | **价格加权分位数优化／MPC**，作为保留不对称经济损失的简化对照 |

上述每行给出的是一条完整主方案和一条备选，而不是要求在同一问堆叠两个独立大模型。预测器负责输入分布，优化模型负责决策，MPC负责更新执行，求解器负责解LP/MILP，这四个层次不能混为一谈。

CVaR暂不进入首选方案。题目要求尽可能节省购电费，主模型先最小化期望费用；只有回测发现少数极端费用日足以改变结论时，才另作风险敏感性分析。LightGBM也不预设为最优，只有结构模型不足且历史验证证明改进后才升级。

## 2. 比初稿更明确的复核结论

### 2.1 负载、光伏和电价应采用不同的基础预测结构

对2025年2月1日—12月31日逐日预测，每一天的预测均仅取更早日期的实测值，不用当天未来值。下面只比较预先列出的简单方法；未进行储能调度或超参数搜索。

| 变量 | 昨日同刻MAE | 上周同刻MAE | 近7日同刻均值MAE |
|---|---:|---:|---:|
| 负载（kW） | 650.02 | **176.80** | 781.21 |
| 光伏（kW） | 185.35 | 214.69 | **153.40** |
| 电价（元/kWh） | 0.08433 | **0.04715** | 0.08267 |

这直接支持：负载和电价优先保留周周期；光伏优先从短期平滑、日内形状和季节变化入手。负载和光伏不宜不加区分地使用同一个“过去7日平均”。

负载用上周同刻、光伏用近7日同刻均值，组合后的净负荷MAE为273.35 kW；直接用上周同刻净负荷为327.77 kW。这支持先分别建模再相减，至少在这组简单候选中如此。

这是一轮用于算法匹配的探索性比较。该比较本身覆盖2—12月，不能随后把它选出的模型重新在同一时期回测，就称为完全独立的最终测试。正式实现时，模型与参数选择须在1月或不断扩展的历史验证窗口内完成，或另留尚未参与选型的后段检验区间。

### 2.2 电价与净负荷存在可利用的关联

用“负载上周同刻＋光伏近7日平均”形成净负荷预测，用“电价上周同刻”形成价格预测，其2—12月预测误差相关系数约为0.603。这个统计是诊断证据，不是因果解释，也不是可以提前知道误差的依据。

另做一个很小的候选验证：用过去最多56天拟合价格周差分对负载、光伏周差分的线性回归，未来负载和光伏用上述历史预测代入。价格MAE约0.04400元/kWh，上周同刻基线为0.04715元/kWh。该结果仅说明简单结构回归值得优先尝试，不能据此宣称已找到最优价格预测器，更不能推断相同比例的电费节省。

### 2.3 附件1接近全年同刻平均，但不得反推题目未给的未来信息

附件1负载与附件2全年同刻平均的最大绝对差约0.0000504 kW；附件1电价与附件4全年同刻平均的最大差约0.0000515元/kWh；光伏对应最大差约0.0431 kW。数值上高度接近，但不能据此断言数据生成方式。

第二、三问明确要求使用附件1的电价，因此使用该电价是题目授权。不能因为附件1同时含有典型负载和光伏，就自行把它们作为第二问的全年未来模板；更不能用全年实测均值训练早期价格模型。算法选型与决策时的信息可用性要分开。

### 2.4 四个容易影响算法结论的口径

- 第一问的储能效率主解释仍为单向各0.9；其他解释作敏感性，而非反复扩大主模型。
- 第三问减购是否退费仍未被题目明确，必须参数化并分别核算，不能用行业习惯替代题目条件。
- 第二至四问跨日传递SOC；是否每天日末闭合属于额外边界设计，不能假装是题目重申的约束。不同方案还要采用一致的年末储能价值比较。
- 原始时刻与结果模板区间存在错位疑点，内部先固定144个标准区间；此轮不修改原始附件或模板。

## 3. 第一问：首选LP，MILP作为严格核对

### 3.1 为什么能明确选择LP

第一问没有随机观测更新，没有调整费用，没有整数台数选择，容量和功率均连续。其本质是带损耗的跨时段能量配置问题。

以十分钟为一个区间，Δt=1/6小时。定义q_t为购电量，c_t、d_t为母线侧充入及放出电量，E_t为电池内部储电量；ℓ_t、v_t分别为区间负载和预测光伏电量，w_t为未利用剩余电量。

$$\min\sum_{t=1}^{144}p_tq_t$$

满足：

$$q_t+v_t+d_t=\ell_t+c_t+w_t,$$
$$E_t=E_{t-1}+0.9c_t-d_t/0.9,$$
$$1200\le E_t\le10800,$$
$$0\le c_t,d_t\le833.333\ldots,$$
$$q_t,w_t\ge0,\qquad E_0=E_{144}=6000.$$

购电量和充放电量都是kWh，不能把5,000 kW直接当成每个区间5,000 kWh的界限。w_t只是对题目供电不低于需求的剩余表示，不含售电收益，也不一定全部是弃光。

### 3.2 LP无需同时充放电的具体证明

若某时段LP解出现c_t>0且d_t>0，令：

$$a=\min\left(c_t,\frac{d_t}{0.81}\right),$$
$$c'_t=c_t-a,\qquad d'_t=d_t-0.81a,\qquad w'_t=w_t+0.19a.$$

则至少一个充放电量变为0，功率上界仍满足，SOC变化保持不变，供需等式仍成立，购电量与购电费完全不变。逐时处理可得到无同时充放电的等价最优解。

因此，在“允许无收益处理剩余电能、没有额外吞吐要求或剩余上限”等当前建模条件下，**LP可以得到与互斥MILP相同的最优购电费**。这个结论来自模型结构，不能直接推广到带额外紧急供电模式或其他限制的后续模型。

### 3.3 第二方案MILP何时值得保留

引入二元变量z_t：

$$c_t\le833.333\ldots z_t,\qquad d_t\le833.333\ldots(1-z_t).$$

这使充放电互斥直接写入模型，不依赖后处理，也便于复用到后续问题。第一问可用它验证LP费用是否一致；不必把LP和MILP包装成两个完全不同的创新模型。

LP采用HiGHS的对偶单纯形或内点法；MILP采用求解器的分支定界／割平面机制。[SciPy线性规划文档](https://docs.scipy.org/doc/scipy/reference/generated/scipy.optimize.linprog.html)和[MILP文档](https://docs.scipy.org/doc/scipy/reference/generated/scipy.optimize.milp.html)提供相应接口说明。此轮没有安装或运行这些求解器。

### 3.4 详细实施思路

1. 确定功率样本的区间归属，生成144组输入电量。
2. 建立上述LP求全日最低购电费，消除退化的循环充放电。
3. 用MILP核对最优费用、储能边界与日末闭合。
4. 分析低价充电、高价放电、午间光伏预留空间各自发挥作用的时段。
5. 与无储能和简单峰谷规则比较，严格统一初末SOC。
6. 输出题目指定十分钟区间、六个4小时充放电合计、全天电量和费用。

不推荐用遗传算法、粒子群替代这个LP。局部的价差阈值p_high>p_low/0.81用于解释机制，不能代替全日受约束优化。

## 4. 第二问：首选因果策略约束的场景随机MILP

### 4.1 这问究竟优化什么

每天0:00选择全天普通购电计划q=(q_1,…,q_144)。日内普通购电计划固定；储能可以按照已观测负载、光伏和SOC执行事先定义的反馈规则；紧急购电只弥补当前负载缺口。

难点是同一块电池既用于价差调度，又用于吸收预测误差。不能简单按各时段80%分位数独立买电，也不能为每条场景单独指定一条预知整天实测的最优电池轨迹。

### 4.2 预测器明确选择周期结构动态回归

负载从上周同刻出发，预测其修正量：

$$\widehat L_{d,t}=L_{d-7,t}+f_\theta(X_{d,t}).$$

f_θ首选带岭正则的线性动态回归，特征仅含决策时已知的信息：近若干完整日的周差分、前一周同刻值、近期日负载水平、日内位置、已知日历属性等。一个共享或少量分时段模型即可，避免初期只有31天却给144个时段分别拟合大量参数。

光伏从近7日同刻平均出发，补充近期日发电量趋势与日间形状修正，限制非负。第三问提供新预报后改为校准现有预报，而非继续仅依赖历史平均。

只用当时已有完整样本训练；预测凌晨之后的时段，不能把该时段前10分钟实际负载当成已知特征。日内允许更新负载预测时，只使用真正已经观测的当天前缀。

### 4.3 场景如何产生

计算历史滚动预测残差，而非同一训练样本的拟合残差。按整天或连续时间块成对抽取负载、光伏残差，加到当天预测上。保留两者关联和连续误差，负载、光伏分别满足非负性后再计算净负荷。

初期从10—20个有效历史误差日开始核查求解规模，历史积累后再增加并检验稳定性。抽样次数不等于独立历史信息量，不能把重复抽样伪装成更多独立数据。场景数量最终由费用与策略稳定性、求解时间决定。

### 4.4 本轮补实：用共享的反馈策略保证因果性

一个可实现的首选参数化是：每天与q_t一起选择备用储量下限r_t，所有场景共享同一组q_t、r_t。控制器当期只看当前净负荷和真实SOC。

设b_t=q_t+v_t−ℓ_t为普通购电和光伏满足负载后的余量，C_max=D_max=833.333… kWh，E_min=1200，E_max=10800。

当b_t≥0：

$$c_t=\min\left\{b_t,C_{max},\frac{E_{max}-E_{t-1}}{0.9}\right\},\qquad d_t=e_t=0.$$

当b_t<0：

$$d_t=\min\left\{-b_t,D_{max},0.9\max(E_{t-1}-r_t,0)\right\},$$
$$e_t=-b_t-d_t,\qquad c_t=0.$$

其中1200≤r_t≤10800。随后按储能状态方程更新E_t。这组规则禁止同时充放电、禁止用紧急购电主动充电；r_t控制是否为后续高风险时段保留电量。

日前目标为：

$$\min_{q,r}\ \sum_t p_tq_t+\sum_s\pi_s\sum_t5p_te_{t,s}+\sum_s\pi_s\Phi(E_{144,s}).$$

每个场景的电池响应必须由同一个反馈规则产生，而不是自由选择。Φ是明确列出的终端价值修正，用于跨日规划；其参数只在历史窗口内确定，实际购电费报表不把Φ混入已支付电费。也可以通过短期跨日展望校验终端处理。

上述max/min及区间模式可用有界分段线性约束和二元变量表达，形成MILP。界限应根据场景需求、储能容量和功率推导，避免任意巨大常数导致数值不稳。还要核查不同备用曲线复杂度和场景规模下的求解时间。

这个方案的优点是：优化时和实际回放时使用同一控制规则，消除了“场景电池提前知道未来”的漏洞。它求的是**所选反馈策略类内、有限场景下的最优解**，不是全部可能控制策略中的全局最优。r_t可以先用六个4小时分段参数减少复杂度，确认收益后再细化。

### 4.5 第二方案：校准分位数预测＋MILP

不显式优化大量日场景，而是用历史预测误差校准净负荷分位数，再求确定性储能调度。备用下限r_t先根据历史验证确定的分时缓冲设定，确定性模型据此安排购电与参考储能状态；运行时仍用第4.4节同一类SOC反馈和缺口补购规则。r_t可由未来高风险窗口累计正误差的历史分位数折算内部能量构造，并限制在储能边界内，不能由当日未来实测倒算。

无储能、固定电价、无剩余价值的单区间模型：

$$\min_{q\ge0}\ pq+5p\,\mathbb E[(N-q)_+].$$

连续分布内点最优条件为F_N(q*)=0.8。故80%分位数可作起始配置；实际需要按白天／夜晚、价格区间和历史覆盖率校准，并让MILP处理跨时段电量耦合。

它的优势是简单、易验证；局限是单点分位数难以表达连续多个时段一起缺电及电池耗尽。因此把它作为主方案的务实对照，不预先称为完整最优策略。

### 4.6 这一问的实际执行顺序

每天先训练／更新预测，再生成场景，优化q与r；冻结普通购电；每十分钟按相同控制规则执行、补缺口并记录状态；当天结束传递真实E_144给次日。1月预热及2月1日初始状态必须统一，不能每天重置6000 kWh。

核心评价为真实总购电费、紧急电量及费用、SOC合规性和日末剩余电量。预测误差只解释中间环节，不替代经济评价。

## 5. 第三问：首选不对称结算下的多阶段随机MPC

### 5.1 先统一结算，再选择策略

令q_t^0为0:00原计划，q_t为最终有效普通购电量，a_t=(q_t−q_t^0)_+，b_t=(q_t^0−q_t)_+。设ρ表示取消部分是否退还原计划款：ρ=0不退款，ρ=1退款。

$$C_t=p_t\left[q_t^0+1.5a_t+(0.5-\rho)b_t+5e_t\right].$$

没有进一步说明时，保留ρ=0为直接字面主口径，ρ=1为敏感性对照；不把任意一种称作已经得到官方确认的解释。

ρ=0且允许处理剩余电能时，主动将最终购电量降到原计划以下被保留原量所支配。模型可以合理地产生零减购，不能为了“有调整”而强迫减购。ρ=1时，减购可能节费。

主口径按每个交付区间最终数量相对凌晨原计划结算一次。中途产生的候选计划不是已交付电量，不能重复收费。若采用逐笔成交解释，需要另建交易账本；本稿不混合两种方式。

### 5.2 预报加工只做必要的校准

附件3的每一个值都要关联“发布时间＋提前小时”，例如6:00的预报1小时对应7:00。先完成时间和跨日索引，再使用分段线性插值形成十分钟输入。首小时要有发布时刻的已观测或已估计锚点。

将现有预报看成主信号，按发布时刻、提前时距和日间位置估计历史偏差，并保留剩余误差场景。初期用分组偏差校准／收缩估计即可，不另训练复杂光伏网络取代题目预报。夜间零值、日出日落附近和18:00—19:00需要单独核查。

### 5.3 为什么选择多阶段随机MPC

0:00买多少电，取决于6:00、12:00、18:00还可付费调整。若凌晨完全忽略后续调整机会，容易过度保守；若假装已经知道后续更新内容，则又过于乐观。多阶段随机模型正好表达这个折中。

对允许更新的三个节点建立缩减场景树，历史场景包括光伏预报更新、负载误差和相应观测前缀。节点决策只能使用节点时已可见的信息；同一历史节点的未来分支共用当前购电与备用决策。

树的节点必须按已经揭示的观测／预报特征划分。不能在6:00用整天最终误差或全天发电量来决定进入哪个分支。也不能认为观察一个连续数值就能识别整条日场景的未来；未来分支必须保留后续不确定性。

为控制规模，可先在6、12、18时各保留2个代表性更新分支，形成最多8条更新路径，再与更细分支核对。分支数只是初始配置；日内十分钟电池响应仍通过因果反馈实现，不建立144层完全展开树。

目标为未来未结算区间的期望购电、调整和紧急费用，加一致的终端修正。已交付区间冻结，已发生成本作为常数保留在总账，不重复影响当前优化。

### 5.4 滚动流程

1. 0:00：用现有预报及历史更新误差构建树，同时考虑后续调整机会，形成当天原计划。
2. 0—6:00：普通购电按当前有效计划执行，电池按因果反馈控制。
3. 6:00：读取真实SOC和新预报，丢弃过时的未来假设，更新剩余场景树，重新求购电与备用计划。
4. 在12:00、18:00重复；日内交易只能在允许时刻修改。
5. 每段只保留实际生效的最终普通购电量；原计划和各次调整日志分别保存，紧急购电另列。

MPC是“预测—优化—执行当前动作—再更新”的控制框架，底层可以是LP/MILP；并不是与MILP竞争的另一类求解器。[滚动优化方法参考](https://web.stanford.edu/~boyd/papers/fast_mpc.html)

采用跨日展望时，次日购电变量只是前瞻计划，正式次日原计划仍在次日0:00生成。缺少2026年初实测时，不能编造跨年回测数据，应使用统一终端处理。

### 5.5 第二方案：确定性MPC＋历史误差缓冲

每次使用最新预报的校准均值或合适分位数，求一个确定性MILP，保留同样的交易规则、终端处理和实际电池控制。它通常更容易实现，能够明确显示滚动更新的价值。

局限是凌晨对未来更新的选项价值表达较弱；更新后的不确定性主要通过缓冲近似。若其实际费用与随机MPC接近，应优先保留更简单方案，不为模型名称复杂而牺牲可复现性。

### 5.6 对预报价值的实验要完整

固定0:00，只选择是否使用6、12、18时预报，共2³=8种组合，都可以在同一批日轨迹上比较。上一版列出的5组不足以覆盖6＋18、12＋18、仅18等交互情况，本轮补为8组。

再用“更新信息但不允许改普通购电，只改电池控制”的对照区分信息收益与交易权限收益。不同组必须采用一致的初末边界、费用口径和训练方式。

6、12时预报的同目标整点MAE显著低于0时，这是已核查的预测证据；是否节费仍取决于电池余量、调整成本和误差方向。18时后的当日整点光伏很小，但18—19时仍有发电，且次日预报会影响跨日留存电量，因此不预先删除18时更新。

没有提供9时、15时等新预报的真实数据，不开展虚构精度的“实证”比较。题目已给时刻的价值分析优先完成。

## 6. 第四问：首选净负荷驱动价格预测＋联合场景优化

### 6.1 信息条件与两个子任务必须保持清楚

主解释为未来价格未知、结算使用对应交付区间的实际价格。若另假设全天价格提前公布，则应独立报告，该条件下只需替换目标系数，不必预测价格。

4-2继续保持每天0:00确定普通购电，日内不能随价格重新挑选普通购电量；4-3允许在规定的预报节点付费调整。实时储能反馈不等于实时普通购电权限。

为比较“价格波动”本身的影响，4-2主对照保持第二问的信息结构，4-3保持第三问的信息结构。如将第四问列出的附件3理解为4-2也可使用，应另标明增加的信息，不能把预测输入改善全归因于价格模型。

### 6.2 价格模型明确选周差分动态回归

推荐的起点为：

$$p_{d,t}-p_{d-7,t}=\beta_0+\beta_L(L_{d,t}-L_{d-7,t})+\beta_P(P_{d,t}-P_{d-7,t})+u_{d,t}.$$

根据历史滚动拟合，使用岭正则控制系数稳定性；日内可用已经观测的价格残差更新短期偏差。预测时右侧当天未来L、P必须用预测值或对应场景值，不能输入当天未来实测。

这一模型同时利用周周期及负载／光伏对价格变化的统计关联。初步历史候选检查支持其可用性，但不对β系数作因果解释，也不预设全年系数固定。

### 6.3 主方案：联合场景，而非独立噪声

生成同一路径上的负载、光伏和价格。可用净负荷相关场景进入价格回归，再成组加入历史剩余误差；对剩余关联仍需检查，不能条件回归后就自动视为独立。当前附件电价均为正，价格场景采用正值建模或仅依据训练窗的校准规则，避免预测形式人为产生与数据不符的负价；不能把全年已知最低价作为早期预测的裁剪边界。

4-2在每个场景下按第二问同一个反馈策略回放，目标为：

$$\min\sum_s\pi_s\sum_t p_{t,s}(q_t+5e_{t,s})+\text{终端修正}.$$

4-3加入节点调整决策并采用第三问的随机MPC：

$$\min\sum_s\pi_s\sum_t p_{t,s}\left[q_t^0+1.5a_{t,s}+(0.5-\rho)b_{t,s}+5e_{t,s}\right]+\text{终端修正}.$$

p_{t,s}是场景参数，乘以决策q_t仍是线性项；随机价格并不要求非线性启发式算法。相关场景用于防止低估“缺电同时高价”的损失。

若改为下单时锁价，则原合约价、各次调整价和紧急交付价必须分别记录；附件未提供独立合约报价，本稿不自行构造新的市场机制。

### 6.4 第二方案：价格加权分位数优化

这比机械沿用第二问普通80%分位数更有针对性。先忽略电池，以随机价格p和净需求N表示单区间成本：

$$C(q)=\mathbb E[p]q+5\mathbb E[p(N-q)_+].$$

连续分布的内点最优条件为：

$$\mathbb E[p\,\mathbf1_{N>q^*}]=\frac{\mathbb E[p]}5.$$

定义价格加权分布：

$$F_p(q)=\frac{\mathbb E[p\,\mathbf1_{N\le q}]}{\mathbb E[p]}.$$

则：

$$F_p(q^*)=0.8.$$

含义是：更贵的缺电场景应有更大权重。如果价格与净负荷独立，才退化为普通80%分位数；相关时不能直接沿用第二问的普通分位数。

在样本中按π_s p_s给净需求场景加权，可求加权分位数，再进入MILP／MPC处理电池耦合。它仍是简化近似，不能把单时段推导冒充有储能、多阶段问题的全局最优，但适合作为第四问紧贴经济机制的对照。

### 6.5 评价应关注哪些量

用同一批实际价格、负载和光伏回放，分解普通购电费、增购费、减购结算和紧急购电费；报告总费、日费分布、紧急电量、SOC和最终剩余电量。比较价格点预测、独立场景、联合场景和价格加权分位数，明确各自贡献。

完全预知未来的优化结果只作为一致约束下的费用下界。可以用它衡量尚有多少改进空间，不能作为可部署策略的成绩。

## 7. 四问统一的落地顺序和取舍规则

1. **先固定模型口径。** 时间区间、效率、SOC初始与终端、剩余能量处理、紧急购电定义、减购退款及交易价格时点。
2. **第一问建立可核查的LP/MILP。** 完成物理和费用审计，作为后续共同底层。
3. **第二问先做分位数简化版，再做因果场景版。** 两者实际回放使用明确且一致的控制权限；场景版不允许隐藏的未来信息。
4. **第三问先做确定性MPC，再增加多阶段场景。** 完成8种更新时间组合及“仅改电池”的信息价值对照。
5. **第四问增加价格回归和联合误差。** 保持4-2、4-3各自权限，比较联合场景与价格加权分位数。
6. **最后决定是否保留复杂增强。** 只有在独立或严格滚动验证下改善总费用、可行性或风险，并且计算量可接受，才进入论文主模型。

本题不需要四套互不相干的大算法。推荐形成一套物理与结算模型，第一问确定求解，第二问加入可执行的随机反馈，第三问加入定时更新的信息结构，第四问加入有相关性的价格风险。这比先指定复杂预测网络、再套一个优化器更有数据依据。
